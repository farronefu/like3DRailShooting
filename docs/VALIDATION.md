# 検証結果

## 使用環境

Godot 4.6 stable official (89cea1439)、GDScript、Compatibility renderer。
Web: Codex内蔵Chromiumブラウザ、1280×720、localhost配信。
Windows: x86_64 release export。

## 自動テスト

`godot --headless --path . --script res://tests/test_game.gd`

**24項目成功、0失敗。**

ゲームパッドのデッドゾーン・斜め入力正規化、物理キー入力、合成ゲームパッド入力の移動・RT射撃、接続対象がない場合の入力解除、移動範囲、射撃・時間の一時停止、再開時の進行保持、ダメージ後無敵、ゲームオーバー、リトライ初期化、敵への命中と得点、敵弾の命中、障害物接触、補給リング、4区間、240秒クリア、クリア後の再開始、メニューへの帰還を検証しました。

フルステージ検証は1/60秒刻みのシミュレーションです。ステージ進行を独立に検証するため、このテストだけはプレイヤーを無敵にし、自動追尾・射撃を入力しています。通常のゲームに無敵機能はありません。

- 結果: 240秒でクリア
- 撃墜: 131機
- 得点: 17,250
- 命中率: 18%
- 敵・弾・障害物・破片の同時存在数の最大: 32

## ビルド

- Web release export: 成功、終了コード0。
- Windows Desktop release export: 成功、終了コード0。
- Windows実行ファイルのheadless起動・120フレーム終了: 終了コード0。
- GDScript構文エラー・実行エラーなし。

検証用サンドボックスではGodot既定のユーザーデータフォルダ作成とWindows証明書ストア読み取りが制限され、エンジン起動時に環境由来のエラーログが出ました。ログを作業フォルダへ出力して実行し、上記のテストと出力は成功しています。本ゲームはユーザーデータ保存・外部ネットワーク接続を使用していません。

## ブラウザの実画面

- WebAssemblyを読み込み、開始画面が表示されることを確認。
- 開始ボタンから飛行画面へ遷移。
- HUD、背景、自機、レティクルを確認。
- ブラウザ実行中に敵の攻撃でHPが減り、撃墜結果画面へ遷移。
- リトライボタンと一時停止画面、再読み込みを確認。
- 開始・一時停止ボタンの不足グリフをASCII表記へ修正。
- 撃墜時の赤いフラッシュが結果画面に残り続ける問題を修正。
- 確認範囲でブラウザコンソールのerror/warnは0件。

## 未検証・今後の確認

- 実物のゲームパッド、各機種のボタン配置、USB接続・切断。
- 人間が通常操作で4分通して遊んだ際の難易度と操作感。
- 低性能PC、Firefox、Safari、Steam Deck、モバイル端末。
- Windows版の描画・音声・ゲームパッドを含む実機プレイ。
- 音声の聴感による音量バランス。
- Steamへの登録、配布、Steam API機能。

自動テストのクリアは、人による難易度評価やすべてのコントローラーでの互換性保証を意味しません。
